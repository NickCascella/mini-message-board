<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <nav>
    <ul>
      <li><a href="/">Message Board</a></li>
      <li><a href="/new">Create a message</a></li>
    </ul>
  </nav>
  <body class="messageBoardDisplay">
    <h1><%= title %></h1>
    <% for(let i = 0; i < messages.length; i++) {%>
    <div class="renderedMessage">
      <span class="messageTitle"><%= messages[i].title %></span>
      <span class="userName"><%= messages[i].user %></span>

      <span class="messageText"><%= messages[i].text %></span>
    </div>
    <% } %>
  </body>
</html>
